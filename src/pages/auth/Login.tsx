import React from "react";
import { css } from "@emotion/react";

const Login: React.FC = () => {
  return (
    <div
      css={css`
        padding: 20px;
      `}
    >
      <h1>Login Page</h1>
    </div>
  );
};

export default Login;
